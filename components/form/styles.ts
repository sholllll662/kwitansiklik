/** Class bersama untuk input teks/select form — fokus terlihat jelas (border + ring), bukan cuma ganti warna border. */
export const TEXT_INPUT_CLASS =
  "rounded-lg border border-foreground/15 px-3 py-2 text-sm outline-none focus:border-foreground/40 focus:ring-2 focus:ring-[#2563EB]/30";
